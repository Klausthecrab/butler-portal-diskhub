# DiskHub Portal — Konfiguration
# Webhook-URL ist hardcoded in routes.py (Schlüsselmeister defekt)

paths:
  repo_dir: "~/repos/butler-portal-diskhub"
  state_db: "~/.hermes/state.db"

polling:
  interval: 5  # Sekunden zwischen Polls
  timeout: 120  # Max Sekunden auf Session warten