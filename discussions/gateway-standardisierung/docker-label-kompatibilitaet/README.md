# Docker-Label-Kompatibilität

**Sub-Diskussion von:** Gateway-Strategie  
**Status:** ● offen

Native Services ohne Docker-Labels müssen via file Provider (YAML) bei Traefik registriert werden.

**Frage:** Soll der file Provider zentral (eine YAML) oder dezentral (pro Service eine YAML) organisiert werden?
